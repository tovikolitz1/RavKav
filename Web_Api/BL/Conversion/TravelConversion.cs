using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using BL.ModelDTO;
using DAL;
using System.Threading.Tasks;

namespace BL.Conversion
{
   public class TravelConversion
    {/*
        public static TravelsDTO convertToTravelsDto(TravelsTbl travelTbl)
        {
            TravelsDTO newTravel = new TravelsDTO
            {
                TransitId = travelTbl.TransitId,
                userId = travelTbl.userId,
                id = travelTbl.id,
                dateAndTime = travelTbl.dateAndTime,
                TransitTbl = travelTbl.TransitTbl,
                UserTbl = travelTbl.UserTbl
            };

            return newTravel;
        }
        public static UserTbl ConvertToTravelTbl(UserDTO userDto)
        {
            UserTbl newUser = new UserTbl();
            newUser.firstName = userDto.firstName;
            newUser.lastName = userDto.lastName;
            newUser.password = userDto.password;
            newUser.profileId = userDto.profileId;
            newUser.ravkav = userDto.ravkav;
            newUser.id = userDto.id;
            newUser.isManager = userDto.isManager;
            return newUser;
        }
 
       */ }
        
}

